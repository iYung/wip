## Configurable Keybinds Checklist

- [ ] Task 1 — `lua/game/settings_state.lua` — In `SettingsState.new()`, add a `keybinds` table field initialised to `{move_up="w", move_down="s", move_left="a", move_right="d", pick_up_down="e", interact="f"}`. Add method `set_keybind(action, key)`: iterate all keys of `self.keybinds`; if any other action already maps to `key`, set that action to `nil`; then set `self.keybinds[action] = key`. Add method `key_map()`: build and return a new table where each action whose binding is non-nil maps to a single-element array `{key}` (actions with `nil` bindings are omitted). No other existing code changes. (No dependencies.)

- [ ] Task 2 — `lua/game/scenes/settings_menu.lua` — Change the `ITEMS` constant from a 3-element list to `{"Fullscreen / Window", "Keybinds", "Exit Settings", "Leave Game"}` (4 elements). Move the `BTN_Y0` calculation inside `SettingsMenu.new()` so it reflects the new count of 4 (currently computed at module load time using `#ITEMS`). Update `SettingsMenu:_confirm()`: item 1 toggles fullscreen (unchanged); item 2 sets `self._subscreen = "keybinds"` and resets `self._subscreen_selected = 1`; item 3 calls `self:close()`; item 4 calls `love.event.quit()`. (No dependencies.)

- [ ] Task 3 — `lua/game/scenes/settings_menu.lua` — Change `SettingsMenu.new(settings_state)` signature to `SettingsMenu.new(settings_state, input)` and store `self._input = input`. Add instance fields `_subscreen = nil`, `_subscreen_selected = 1`, and `_capturing = nil` in `new()`. (Depends on Task 2.)

- [ ] Task 4 — `lua/game/scenes/settings_menu.lua` — Define module-local constants `_ACTION_LIST = {"move_up","move_down","move_left","move_right","pick_up_down","interact"}` and `_ACTION_LABELS = {"move up","move down","move left","move right","pick up/down","interact"}`. Extend `SettingsMenu:update(dt)` to handle the keybind sub-screen. When `self._subscreen == "keybinds"` and `self._capturing == nil`: read up/down using `self._state.keybinds.move_up` / `move_down` (plus `"up"`/`"down"` arrows) with the same edge-trigger pattern as the main screen; move `self._subscreen_selected` within `[1..6]` wrapping; on confirm (using the remapped `pick_up_down` or `interact` keys, or `"return"`/`"space"`), set `self._capturing = _ACTION_LIST[self._subscreen_selected]`; on escape, clear `self._subscreen` and return to main screen. When `self._capturing ~= nil`, suppress all update logic (navigation frozen). When `self._subscreen == nil`, existing main-screen logic runs unchanged. (Depends on Tasks 2 and 3.)

- [ ] Task 5 — `lua/game/scenes/settings_menu.lua` — Add `SettingsMenu:keypressed(key)`. Define a module-local set `_MODIFIERS = {lshift=true, rshift=true, lctrl=true, rctrl=true, lalt=true, ralt=true, lgui=true, rgui=true, capslock=true, numlock=true, scrolllock=true}`. If `self._capturing ~= nil`: if `key == "escape"`, clear `self._capturing` and return; if `_MODIFIERS[key]`, return (ignore); otherwise call `self._state:set_keybind(self._capturing, key)`, set `self._input._map = self._state:key_map()`, clear `self._capturing`, and return. If `self._capturing == nil`, return immediately (no-op). (Depends on Tasks 1, 3, and 4.)

- [ ] Task 6 — `lua/game/scenes/settings_menu.lua` — Extend `SettingsMenu:draw()` to render the keybind sub-screen when `self._subscreen == "keybinds"`. For each row `i` (1–6), draw the button image (selected image for `_subscreen_selected` row), then `_ACTION_LABELS[i]` left-aligned and `"[" .. (self._state.keybinds[_ACTION_LIST[i]] or "unbound"):upper() .. "]"` right-aligned within `BTN_W`; if `self._capturing == _ACTION_LIST[i]`, show `"[press a key]"` instead of the key name. Reuse existing layout constants (`BTN_X`, `BTN_W`, `BTN_H`, `BTN_GAP`), images, and font. (Depends on Tasks 2, 3, and 4.)

- [ ] Task 7 — `main.lua` — In `love.load()`, change `SettingsMenu.new(ss)` to `SettingsMenu.new(ss, input)`. In `love.keypressed(key)`, add at the very top of the function body, before the existing `if key == "escape"` block: `if settings_menu and settings_menu.is_open then settings_menu:keypressed(key) end`. (Depends on Tasks 3 and 5.)

- [ ] Task 8 — `tests/test_settings_state.lua` — Append new tests after the existing three without modifying them. Add: (a) keybind defaults — assert all six default bindings; (b) `set_keybind` basic — call `set_keybind("move_up", "t")`, assert result; (c) collision clearing — bind `"w"` to `move_up`, then `set_keybind("move_down", "w")`, assert `move_up` is nil and `move_down` is `"w"`; (d) `key_map` output — assert action maps to single-element array; (e) `key_map` skips nil bindings — set a binding to nil, assert that action is absent from `key_map()`. (Depends on Task 1.)

- [ ] Task 9 — `tests/test_settings_menu.lua` — Fix existing tests broken by the new 4-item list: test 7 wrap now goes from 4 to 1; test 8 up-wrap goes to 4; tests 14–16 index references shift (Exit Settings = 3, Leave Game = 4). Add new tests: (a) item count wraps at 4; (b) selecting item 2 opens keybind sub-screen (`m._subscreen == "keybinds"`); (c) escape from sub-screen returns to main (`_subscreen == nil`, menu still open); (d) confirm in sub-screen enters capture mode (`_capturing == "move_up"`); (e) `keypressed` sets binding and clears `_capturing`; (f) `keypressed` with modifier is ignored (capturing remains set); (g) `keypressed` escape cancels capture without changing binding; (h) collision clears old binding via `keypressed`. For tests (e)–(h), construct `SettingsMenu.new(state, {_map={}})` as the fake input. (Depends on Tasks 1–6.)
