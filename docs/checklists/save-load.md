## Save / Load Checklist

- [x] Task A — `lua/game/save.lua` (new) — create Save module with `exists()`, `write(data)`, `read()`. `write` takes a plain table and serializes it to `save.dat` using a recursive Lua-table serializer and `love.filesystem.write`. `read` loads via `love.filesystem.read` + `load("return "..content)()`, returning nil if file missing or corrupt. `exists` uses `love.filesystem.getInfo`.

- [x] Task B — `lua/game/game_state.lua` — add `GameState.to_save(gs)` which extracts a plain serializable table (all scalar fields, slot count, per-slot item records, player x/facing/held_item). Add `GameState.from_save(data)` which reconstructs a live GameState: creates Store with correct slot count, calls local `_item_from_data(d)` for each slot and held_item. `_item_from_data` dispatches on `d.type`: plant → `Plant.new(plant_type)` + set stage + `sprite:set(tostring(stage))`, others → their respective `.new()`. PCStore is created with `nil` factory (wired later). Requires importing Plant, WateringCan, Grafter, GarbageBin, PCStore at top of file.

- [x] Task C — `lua/game/scenes/store_scene.lua` — add `from_save` boolean param to `StoreScene.new()` (default false). In `_setup_store()`, gate the default item placement (slots 1–3) behind `not self._from_save`. Add `_wire_pc_store()` which iterates all slots and `player.held_item` to find a PCStore and sets its `buy_scene_factory` closure (same factory logic as the existing placement code). Call `_wire_pc_store()` at the end of `_setup_store()` when `self._from_save` is true.

- [x] Task D — `lua/game/scenes/start_scene.lua` — in `on_enter()` call `Save.exists()` and store as `self._has_save`. In `_confirm()`: selected==1 (New Game) calls `GameState.new()` → `StoreScene.new(gs, input, sm, false)`; selected==2 (Continue) is a no-op if `not self._has_save`, otherwise calls `Save.read()` → `GameState.from_save(data)` → `StoreScene.new(gs, input, sm, true)`. In `draw()`, render the Continue button at 50% alpha when `not self._has_save`.

- [x] Task E — `lua/game/scenes/settings_menu.lua` — add `"Save Game"` to `ITEMS` between "Keybinds" and "Exit Settings". Accept an `on_save` callback as third arg to `SettingsMenu.new()`. In `_confirm()`, handle the new item index: call `on_save()` if `not self._opaque` (in-game only); if `self._opaque` (start screen), do nothing. In `draw()`, render the item grayed (`setColor(0.5,0.5,0.5,1)`) when `self._opaque`.

- [x] Task F — `main.lua` — pass save callback as third arg to `SettingsMenu.new()`: the callback calls `Save.write(GameState.to_save(scene_manager.current.game_state))` guarded by `scene_manager and scene_manager.current and scene_manager.current.game_state`. Add `love.quit()` with the same guard and save call. Require `Save` and ensure `GameState.to_save` is accessible.
