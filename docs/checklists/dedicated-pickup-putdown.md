## Dedicated Pick Up / Put Down Checklist

- [ ] Task A — `lua/game/input.lua` — Add `pick_up_down = {"o"}` to the key map (after `interact`). In `_PAD_LABELS` add `pick_up_down = "[B]"`. In `Input:update()` add `pick_up_down = joy:isGamepadDown("b")` to the `pad` table so the B button fires the action.

- [ ] Task B — `lua/game/settings_state.lua` — Add `pick_up_down="o"` to `self.keybinds` in `SettingsState.new()` (line 11); the existing `from_save` guard already handles old saves safely

- [ ] Task C — `lua/game/scenes/store_scene.lua` — In `update()` (line 348) replace the two `move_up`/`move_down` pressed checks with one: `if input:pressed("pick_up_down") then self:_handle_pick_up_down() end`. Then merge `_handle_pick_up()` (line 364) and `_handle_put_down()` (line 399) into a single `_handle_pick_up_down()` with this priority order: (1) cashier zone (player.x < 0) → dismiss customer if arrived and not `no_dismiss`; (2) holding + carriable slot item → swap; (3) not holding + carriable slot item → pick up; (4) holding + empty slot → put down. Sound calls stay unchanged.

- [ ] Task D — `lua/game/scenes/store_scene.lua` — In `_hud_labels()` (line 495) replace the `up_key`/`down_key` pair (from `move_up`/`move_down`) with a single `carry_key = (self.input:key_for("pick_up_down") or "o"):upper()`. Update all label strings: replace `up_key .. "/" .. down_key .. ": DISMISS"` with `carry_key .. ": DISMISS"`; replace `up_key .. "/" .. down_key .. ": SWAP WITH ..."` with `carry_key .. ": SWAP WITH ..."`; replace `up_key .. ": PICK UP"` with `carry_key .. ": PICK UP"`; replace `down_key .. ": PUT DOWN"` with `carry_key .. ": PUT DOWN"`.

- [ ] Task E — `lua/game/scenes/buy_scene.lua` — In `update()` (line 128) replace `input:pressed("move_down")` with `input:pressed("pick_up_down")`. On line 405 replace `self.input:key_for("move_down")` with `self.input:key_for("pick_up_down")` (and update the fallback from `"o"` to `"o"` — no change needed there).

- [ ] Task F — `lua/game/scenes/settings_menu.lua` — Replace `_ACTION_LIST` and `_ACTION_LABELS` (lines 14–15) with 4-entry versions that drop `move_up`/`move_down` and add `pick_up_down`: `{"pick_up_down","move_left","move_right","interact"}` / `{"Pick Up / Put Down","Left","Right","Interact"}`. `_sub_btn_y0` is already dynamic (`#_ACTION_LIST`) so it self-corrects to 4 rows. No changes needed to the `move_up`/`move_down` snapshot reads — those navigate the settings menu itself and stay on W/S. Also remove B button as "back" from `_joy_nav`: set `back = false` (or remove the field) on line 43, remove `back` from the empty-joystick return on line 32. Then remove every place `_jn.back` / `_prev_back` / `_prev_sub_back` is read or written: lines 74–75 (`self._prev_back/sub_back = false` init), 126 (`self._prev_back = _jn.back`), 159–166 (B cancels capture block), 197 (`self._prev_sub_back`), 214 (`escape = escape or _jn.back`), 268 (`self._prev_back`), 284–285 (`_prev_sub_back/_prev_back` in confirm snapshot).
