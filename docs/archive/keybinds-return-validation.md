## Keybinds Return Validation Checklist

- [x] Task 1 — `lua/game/scenes/settings_menu.lua` — Add a module-local helper `local function _all_bound(keybinds) ... end` that iterates `_ACTION_LIST` and returns `false` if any `keybinds[action]` is nil, otherwise `true`. No other changes.

- [x] Task 2 — `lua/game/scenes/settings_menu.lua` — In `SettingsMenu:update()`, inside the `self._subscreen == "keybinds"` branch (where `self._capturing == nil`): gate the two exit paths with `_all_bound(self._state.keybinds)`. For the confirm path (`confirm and not self._prev_sub_confirm` and `self._subscreen_selected == sub_count`), only set `self._subscreen = nil` when `_all_bound` is true; otherwise do nothing. For the escape path (`escape and not self._prev_sub_escape`), only set `self._subscreen = nil` when `_all_bound` is true; otherwise do nothing. (Depends on Task 1.)

- [x] Task 3 — `lua/game/scenes/settings_menu.lua` — In `SettingsMenu:keypressed()`, inside the block `if self._subscreen == "keybinds" and self._capturing == nil`, change the `key == "escape"` handler to only set `self._subscreen = nil` (and return true) when `_all_bound(self._state.keybinds)` is true; otherwise return false. (Depends on Task 1.)

- [x] Task 4 — `lua/game/scenes/settings_menu.lua` — In `SettingsMenu:draw()`, inside the `self._subscreen == "keybinds"` branch, update the Return button rendering. Compute `local all_ok = _all_bound(self._state.keybinds)`. When `all_ok` is false: draw `self._img_btn` (never the selected image) at `setColor(1,1,1,0.4)`, draw the "Return" label at `setColor(1,1,1,0.4)`, then draw a hint line `"all keys must be bound"` using `self._font_vol`, color `(1, 0.35, 0.35, 1)`, centered over `BTN_W` at `y = ry + BTN_H + 6`. When `all_ok` is true: render exactly as before (selected/unselected image at full alpha, label at full alpha, no hint text). (Depends on Task 1.)

- [x] Task 5 — `tests/test_settings_menu.lua` — Add new tests (after existing ones, without modifying them): (a) when all keybinds are set, confirming Return on the sub-screen closes it (`_subscreen == nil`); (b) when one keybind is nil, confirming Return does not close the sub-screen; (c) when one keybind is nil, pressing escape (via `keypressed`) does not close the sub-screen; (d) once the missing keybind is restored, confirming Return closes the sub-screen. (Depends on Tasks 1–3.)
