## Custom Icon and Title Checklist

- [x] Task A — `conf.lua` — change `t.window.title` from `"plant game"` to `"Frobert"`
- [x] Task B — `main.lua` — at the top of `love.load()` (before the `if _visual_mode` branch), load `assets/images/icon.png` as ImageData and pass it to `love.window.setIcon()`
