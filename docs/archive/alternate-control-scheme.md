## Alternate Control Scheme Checklist

- [x] Task 1 — `lua/game/settings_state.lua` — Add `control_scheme = "up_down"` field to `SettingsState.new()`. Add `pick_up = "o"` and `put_down = "p"` to `self.keybinds`. Include `control_scheme` in `to_save()`. In `from_save()`, restore `control_scheme` from `data.control_scheme` if it equals `"pick_put"`, otherwise keep default `"up_down"`.

- [x] Task 2 — `lua/game/input.lua` — Add `pick_up = {"o"}` and `put_down = {"p"}` to the default `Input.new({...})` call. Also add a `control_scheme = "up_down"` field to `Input.new()` (stored as `self.control_scheme`) so scenes can read it without needing a reference to `SettingsState`.

- [x] Task 3 — `main.lua` — Depends on Tasks 1 and 2. After the line `input._map = ss:key_map()`, add `input.control_scheme = ss.control_scheme` so the scheme is initialised from saved settings on startup.

- [x] Task 4 — `lua/game/scenes/settings_menu.lua` — Depends on Tasks 1 and 2. Update the keybinds sub-screen to add a "Control Scheme" toggle row at the top (row 0, before the 5 bindable key rows). When this row is confirmed, toggle `self._state.control_scheme` between `"up_down"` and `"pick_put"`, call `self._input._map = self._state:key_map()`, and set `self._input.control_scheme = self._state.control_scheme`. The five key rows change per scheme: `"up_down"` shows actions `{move_up, move_down, move_left, move_right, interact}` labelled `{"Up","Down","Left","Right","Interact"}`; `"pick_put"` shows `{pick_up, put_down, move_left, move_right, interact}` labelled `{"Pick Up","Put Down","Left","Right","Interact"}`. Sub-screen total row count is now 7 (1 toggle + 5 key rows + 1 Return). Navigation, `_all_bound` check, and draw all update to account for the new row. The toggle row draws as a label+value bar pair: label "Control Scheme", value "Up / Down" or "Pick Up / Put Down".

- [x] Task 5 — `lua/game/scenes/store_scene.lua` — Depends on Tasks 1 and 2. In `update()`, replace the existing `input:pressed("move_up")` / `input:pressed("move_down")` block with: if `self.input.control_scheme == "pick_put"`, check `input:pressed("pick_up")` / `input:pressed("put_down")`; otherwise keep `move_up`/`move_down`. In `_hud_labels()`, when `self.input.control_scheme == "pick_put"`, use `input:key_for("pick_up")` and `input:key_for("put_down")` for the pick/put key labels, and omit the dismiss label for the cashier zone (since W/S are inert in this scheme).
