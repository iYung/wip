# Controller Start as Escape Checklist

- [x] Task A — `lua/game/scenes/settings_menu.lua` — Add `SettingsMenu:gamepadpressed(button)` method (place it right after `SettingsMenu:keypressed`). It mirrors `keypressed` exactly: if `_capturing ~= nil` and button is `"start"`, set `_capturing = nil` and return `true`; if `_subscreen == "keybinds"` and button is `"start"`, set `_subscreen = nil` (only if `_all_bound`) and return `true`; otherwise if button is `"start"`, call `self:close()` and return `true`; return `false` for all other buttons. Also, inside the `_subscreen == "keybinds"` block in `update()`, merge Start into the `escape` local the same way the main-menu level does: `local escape = love.keyboard.isDown("escape") or (self._input._joystick ~= nil and self._input._joystick:isConnected() and self._input._joystick:isGamepadDown("start"))`.

- [x] Task B — `main.lua` — In `love.gamepadpressed`, add a delegation block before the `if button == "start"` branch: `if settings_menu and settings_menu.is_open then if settings_menu:gamepadpressed(button) then return end end`. Then inside the `button == "start"` branch, remove the direct `settings_menu:close()` call (the new method handles that path); keep only the `settings_menu:open()` call for when the menu is not open. Task B depends on Task A.
