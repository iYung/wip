## Faster First Customer Entrance Checklist

- [x] Task A — `lua/game/scenes/store_scene.lua` — In `StoreScene:_setup_store` (around line 109), change `self._spawn_timer = Timer.new(spawn_cooldown(gs))` so that on a brand new game (`not self._from_save`) it is created with a short interval (`1` second) instead of `spawn_cooldown(gs)`. When `self._from_save` is true, keep using `spawn_cooldown(gs)` exactly as today. Do not change `target_x`, `exit_x`, `customer_walk_speed`, or any other spawn parameters. (The existing `self._spawn_timer:reset(cd)` in `StoreScene:update` around line 311 already restores the normal `spawn_cooldown(gs)` value the first time the timer fires, so no change is needed there — verify this remains true after your edit.)
- [x] Task B — `tests/test_customer_scripts.lua` or `tests/test_quest_timing.lua` — Add a test asserting that on a brand new game (`from_save` falsy/omitted), the first customer (`sage:1`) becomes active sooner than the normal 4-second cooldown would allow (e.g. fast-forward ~1–1.5s and assert the customer is active/visible, rather than needing the full 4s). Also add/keep a test asserting that loading from a save still uses the normal `spawn_cooldown` for the initial timer (i.e. the first spawn does NOT happen early when `from_save` is true). Use the existing `runner.fast_forward_until` helpers and patterns already present in these test files.
