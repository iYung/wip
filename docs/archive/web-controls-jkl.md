## Web Controls JKL Checklist

- [x] Task A — `web-template/controls.js` — In the `KEY_CODES` map (around line 199), remove the `' ': 32` (Space) and `'Escape': 27` entries and add `'j': 74, 'k': 75, 'l': 76`. In the CSS style array, change `.cluster-right` grid from `grid-template-columns: repeat(2, 60px); grid-template-rows: repeat(2, 60px)` to `grid-template-columns: repeat(3, 60px); grid-template-rows: repeat(1, 60px)` (one row of three buttons). Replace the `.btn-space` rule (which had `grid-column: 1 / span 2; grid-row: 1`) with three rules: `.btn-j { grid-column: 1; grid-row: 1; }`, `.btn-k { grid-column: 2; grid-row: 1; }`, `.btn-l { grid-column: 3; grid-row: 1; }`. Delete the `.btn-esc` rule entirely.

- [x] Task B — `web-template/controls.js` — In the DOM-building section, replace the two button elements (`btnSpace` firing `' '`/`Space` and `btnEsc` firing `'Escape'`/`Escape`) with three new button elements: `btnJ` (class `btn-j`, label `J`, fires `j`/`KeyJ`), `btnK` (class `btn-k`, label `K`, fires `k`/`KeyK`), `btnL` (class `btn-l`, label `L`, fires `l`/`KeyL`). Update `rightCluster.appendChild` calls accordingly. Run sequentially after Task A since both touch the same file.
