## HUD Box Checklist

- [x] Task A — `lua/game/ui.lua` (new), `lua/game/customer.lua` — Create `lua/game/ui.lua` that exports `draw9` (extracted verbatim from the local in `customer.lua`, lines 21–39) and exports `draw_hud_box(labels, font)`. `draw_hud_box` measures the widest label with `font:getWidth`, computes a box anchored 10 px from the left and 10 px from the bottom of the 1280×720 logical canvas (padding of 14 px on all sides, same as `PAD` in customer.lua), draws the `A.speech_bubble` 9-slice with margin `{ top=12, right=12, bottom=12, left=12 }`, and returns immediately if `#labels == 0`. Update `customer.lua` to require `lua/game/ui.lua` and replace its local `draw9` call with the shared one; remove the local definition.

- [x] Task B — `lua/game/scenes/store_scene.lua` — Require `lua/game/ui.lua`. In `StoreScene:draw()`, after building the `labels` table (line 526), call `ui.draw_hud_box(labels, love.graphics.getFont())` before the existing `love.graphics.print` loop. Keep the print loop exactly as-is so text renders on top of the box. Remove the bare `love.graphics.setColor` call that precedes the print loop only if it is no longer needed (the box draw resets color; verify). Task B depends on Task A completing first.
