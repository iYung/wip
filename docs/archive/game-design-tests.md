## Game Design Tests Checklist

- [x] Task 1 — `lua/headless/runner.lua` — Promote `fast_forward_until` from `test_golden_lotus.lua` into `runner.lua` as `runner.fast_forward_until(ctx, condition_fn, elapsed, cap)`. The function advances the simulation one tick at a time (dt=1.0) until `condition_fn()` returns true, incrementing `elapsed` each tick, and errors if `cap` (default 600) iterations are exceeded. Return the updated `elapsed`. This is a prerequisite for Tasks 2–5.

- [x] Task 2 — `tests/test_balance.lua` — Implement **Test 1: Progression pace**. Create the file (shared boilerplate: require runner + StoreScene, define local `walk_to` and `sell_plant` helpers copied from `test_golden_lotus.lua`). Set up a single context: `ctx.gs.currency = 0`, `ctx.gs.unlocked_plants = { [1] = true }`, plant a grass plant (stage 1) in `ctx.gs.store.slots[4]` directly (`slot.item = { plant_type=1, stage=1, ready=false, ... }`). For each target cost (`{cactus=3, rose=6, tulip=10, daisy=15, golden_lotus=20}`), run the water-walk-sell loop using `runner.fast_forward_until` + `walk_to` + `sell_plant` and record `elapsed` when `ctx.gs.currency >= cost`. Print the labelled result block shown in the design doc. No asserts. Depends on Task 1.

- [x] Task 3 — `tests/test_balance.lua` — Implement **Test 2: Gold-per-minute per plant**. For each plant type (1–6), create a fresh context, set `ctx.gs.currency = 999999`, `ctx.gs.unlocked_plants = { [pt] = true }`, plant type `pt` at stage 1 in slot 4, seed `math.randomseed(42)`. Record `start_currency`. Drive the perfect loop (fast-forward until plant ready → walk to plant → water → walk to cashier → sell matching customer) for 60 simulated seconds using `runner.fast_forward_until`. Compute `gpm = end_currency - start_currency`. Print the labelled table from the design doc. No asserts. Depends on Task 1; can run in parallel with Task 2 if appended to the same file by different agents — but since both write to `test_balance.lua`, run sequentially after Task 2.

- [x] Task 4 — `tests/test_balance.lua` — Implement **Test 4: Growth multiplier value**. For each tier (`{mult=1.0, level=0, cost=0}, {1.25, 1, 20}, {1.60, 2, 50}, {2.00, 3, 100}`) create a fresh context, set `ctx.gs.growth_mult = mult`, `ctx.gs.growth_level = level`, seed `math.randomseed(42)`, golden lotus (plant_type=6, stage=1) in slot 4. Run the water-sell loop for 300 simulated seconds; track total gold earned vs the base-tier run. For tiers 1–3 compute payback time by tracking the simulated second when cumulative extra gold first meets or exceeds tier cost. Print the labelled table from the design doc. No asserts. Depends on Task 1; append to `test_balance.lua` after Task 3.

- [x] Task 5 — `tests/test_balance.lua` — Implement **Test 5: Speed upgrade ROI**. For each speed tier (0–3), create a fresh context, set `ctx.gs.speed_level = tier` and `ctx.gs.player.speed` to the matching value from `speed_tiers.lua` (`{[0]=220,[1]=320,[2]=480,[3]=720}`), seed `math.randomseed(42)`, golden lotus (plant_type=6, stage=1) in slot 4. Run the water-sell loop for 3600 simulated seconds. Count sales by watching `ctx.gs.currency` delta (each golden lotus sale adds $40). For tiers 1–3 compute payback time in minutes when cumulative extra gold exceeds tier cost (`{15, 40, 100}`). Print the labelled table from the design doc. No asserts. Depends on Task 1; append to `test_balance.lua` after Task 4.
