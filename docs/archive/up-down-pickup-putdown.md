## Up/Down Pick Up / Put Down Checklist

- [ ] Task A — `lua/game/input.lua` — Remove `pick_up_down = {"o"}` from the default key map
- [ ] Task B — `lua/game/settings_state.lua` — Remove `pick_up_down` from default keybinds in `SettingsState.new()`; no other changes needed (from_save guard and to_save iterator handle old saves automatically)
- [ ] Task C — `lua/game/scenes/store_scene.lua` — Replace the single `pick_up_down` press check in `update()` with two checks: `move_up` pressed calls `_handle_pick_up()` and `move_down` pressed calls `_handle_put_down()`. Rename/split `_handle_pick_up_down()` into two functions: `_handle_pick_up()` (no-held → pick from slot; held + carriable slot → swap) and `_handle_put_down()` (held + empty slot → put down; held + carriable slot → swap). In the cashier zone (player.x < 0), both `move_up` and `move_down` dismiss the customer.
- [ ] Task D — `lua/game/scenes/store_scene.lua` — Update `_hud_labels()`: replace the single `e_key`/`e_label` with separate `up_key`/`down_key` and `up_label`/`down_label` entries. Show `up_key .. ": PICK UP"` when not holding and slot has carriable item; `down_key .. ": PUT DOWN"` when holding and slot is empty; `up_key .. "/" .. down_key .. ": SWAP"` when holding and slot has carriable item; `up_key .. "/" .. down_key .. ": DISMISS"` in cashier zone with customer. Return both from `_hud_labels()` and include both in the HUD box render in `draw()`.
- [ ] Task E — `lua/game/scenes/buy_scene.lua` — Replace `input:pressed("pick_up_down")` (which returns to store) with `input:pressed("move_down")`; update the `e_key` HUD display on line 405 to use `move_down` key
- [ ] Task F — `lua/game/scenes/start_scene.lua` — Remove `pick_up_down` from the confirm check in `update()` (keep `interact` only); remove the `kp` key display block in `draw()` (lines 176-178)
- [ ] Task G — `lua/game/scenes/settings_menu.lua` — Remove `pick_up_down` from `_ACTION_LIST` and `_ACTION_LABELS` (6 → 5 entries); remove all `pick_up_down` references from `confirm` key checks in `open()`, `update()`, and `_confirm()` (keep `interact` + return/space); update `_sub_btn_y0` to center 5 rows instead of 6
